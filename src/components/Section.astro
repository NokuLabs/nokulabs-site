---
const { kicker, title } = Astro.props;
---

<section class="mt-14">
  {kicker && <p class="text-xs uppercase tracking-[0.18em] text-white/45">{kicker}</p>}
  {title && <h2 class="mt-3 text-2xl font-semibold text-white">{title}</h2>}
  <div class="mt-6">
    <slot />
  </div>
</section>